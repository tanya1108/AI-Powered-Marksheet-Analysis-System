import React from "react";
import { ToastContainer as BaseToastContainer } from "react-toastify";
import "react-toastify/dist/ReactToastify.css";

const Toast = () => {
  return <BaseToastContainer position="bottom-left" />;
};

export default Toast;
